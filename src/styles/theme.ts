import { DefaultTheme } from "styled-components";

const fontSize = {
  xs: "0.5rem",
  sm: "0.75rem",
  base: "1rem",
  md: "1.25rem",
  lg: "1.5rem",
};

const colors = {
  lightBlack: "#2D3036",
  black: "#1F2228",
  gray: "#57595D",
  blue: "#389CFF",
  white: "whitesmoke",
};

const theme: DefaultTheme = {
  fontSize,
  colors,
};

export default theme;
